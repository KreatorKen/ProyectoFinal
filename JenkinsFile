pipeline {
    agent any
    tools {
        msbuild 'VisualStudio 2022'
    }
    stages {
        stage('Build') {
            steps {
                script {
                    bat 'prjCalculadora.sln /p:Configuration=Release %MSBUILD_ARGS%'
                }
            }
        }
    }
}